//
// Created by mavri on 16 Jan 2025.
//

#include "../../include/Interface/AlertWidget.h"

/*
 *TLDR: A box that pops up over everything else and the background is blurred
 *It had a set timer that it can be seen for (it closes automatically after that)
 *It should have a function that accepts a string and displays it
 */
